// UTMS Prisma schema placeholder.
// Concrete models should be added by backend module migrations.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
